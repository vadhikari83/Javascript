'use strict'; //Strict mode implies that all code should run with newer versions.
//alert("Hello")
/* we are using nodejs not browser, so we can not use alert statement as it is.*/ 
console.log("DataTypes");
let yourName = "Virendra";
let yourAge = 23;
let hasLicense = true;


console.log(`

    - Number data types used to store numerical data. Floating or fraction also use the same data type.
    - string data type used to store series of characters defined in single/double qoutes.
    - boolean is the datatype used to store true or false.
    - undefined is also a datatype.
    - null datatype is standalone value.
    - symbol => unique
    - object 
    
    
    
    `);

    console.log(typeof(null));
